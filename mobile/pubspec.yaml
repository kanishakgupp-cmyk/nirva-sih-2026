name: nirva

description: NIRVA field evidence and verification companion.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  crypto: ^3.0.6
  geolocator: ^14.0.2
  http: ^1.2.2
  image: ^4.5.4
  mobile_scanner: ^7.0.1
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
